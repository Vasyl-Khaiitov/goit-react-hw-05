export default function MovieList() {
  return (
    <div>
      {/* <ul>
        {movies.map((movie) => (
          <li key={movie.id}>{movie.title}</li>
        ))}
      </ul> */}
      <ul>
        <li>
          <strong>here movies </strong>
        </li>
      </ul>
    </div>
  );
}
